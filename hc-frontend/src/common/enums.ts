export enum AuthStep {
  PHONE = "phone",
  OTP = "otp",
  CREATE_USER = "createUser",
  EMPTY = "",
}

export enum PropertyListingType {
  DIY = "DIY",
  CALL = "CALL",
}

export enum PropertyType {
  RENT = "RENT",
  RESALE = "RESALE",
  FLATMATES = "FLATMATES",
}
