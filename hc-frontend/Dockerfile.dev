# Use a lightweight Node image
FROM node:22-alpine

WORKDIR /app

# 1. Install dependencies
COPY package.json package-lock.json ./
RUN npm install

# 2. Bind Mount placeholder (Explicitly handled in compose)
EXPOSE 3000
ENV HOSTNAME=0.0.0.0

CMD ["npm", "run", "dev:local"]