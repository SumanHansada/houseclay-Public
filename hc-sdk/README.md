# hc-sdk

Shared SDK for the Houseclay platform — intended to house shared TypeScript types, API client helpers, and utilities reusable across `hc-frontend`, `zebra-ui`, and `hc-mobile-app`.

**Status: In progress.** The directory is currently a placeholder. No code has been published yet.
